module.exports = {
  apps: [{
    name: 'quantum-trading-suite',
    script: 'npm',
    args: 'start',  // Use production mode
    cwd: '/home/user/webapp',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    env_production: {
      NODE_ENV: 'production',
      PORT: 3000
    }
  }]
};